# Gaustering: LiDAR Point Cloud to 2D Gaussian Surfel Clustering

## 0. Overview

### 문제 정의

자율주행 차량의 LiDAR 센서는 매 프레임 약 34,000개의 3D 점(point cloud)을 생성한다. 이 점들은 주변 환경의 표면(도로, 건물, 차량 등)에서 반사되어 돌아온 레이저 펄스의 위치를 나타낸다.

**목표:** 이 점들을 의미 있는 그룹(cluster)으로 묶고, 각 그룹을 하나의 **2D 가우시안 서펠(surfel)**로 요약하는 것이다.

### 2D 가우시안 서펠이란?

서펠(surfel = surface element)은 표면의 국소적 조각을 나타내는 원소이다. 2D 가우시안 서펠은 다음으로 정의된다:

```
서펠 = {
    위치(position):    μ ∈ R³       — 서펠의 중심
    방향(rotation):    q ∈ R⁴       — 쿼터니언 (탄젠트 프레임의 회전)
    크기(scaling):     s ∈ R²       — 장축/단축 반지름
    법선(normal):      n ∈ R³       — 표면 법선 벡터
    불투명도(opacity): α ∈ [0, 1]   — 렌더링 시 투명도
}
```

직관적으로, 각 서펠은 3D 공간에 떠 있는 **타원형 디스크**이다. 이 디스크는 표면의 작은 조각을 근사한다.

### 왜 서펠인가?

3D Gaussian Splatting(3DGS)은 장면을 수천~수만 개의 가우시안으로 표현하여 Novel View Synthesis(NVS)를 수행한다. 서펠은 이 가우시안들의 **초기값(initialization)**으로 사용된다. 좋은 초기값이 있으면 네트워크의 수렴이 빠르고, 렌더링 품질이 높아진다.

### 파이프라인 전체 흐름

```
입력: .pcd.bin (LiDAR 1프레임, ~34k 점)
  │
  ├── Stage 0: 전처리 — 자차 제거, 이상치 제거
  ├── Stage 1: 국소 기하 추정 — 법선, 곡률, 평탄도
  ├── Stage 2: 시드 배치 — 지면 검출, 적응형 복셀 시딩
  ├── Stage 3: 포인트 할당 — 비용 함수 기반 클러스터링
  ├── Stage 4: 리파인먼트 — Split/Merge 반복 정제
  └── Stage 5: 가우시안 피팅 — 각 클러스터 → 2D 가우시안
  │
출력: K개의 2D 가우시안 서펠 파라미터
```

### nuScenes 좌표계

본 파이프라인이 사용하는 nuScenes 데이터셋의 LiDAR 좌표계:

```
X: 전방 (forward)
Y: 좌측 (left)
Z: 위쪽 (up)        ← 센서 원점은 차량 루프탑, 지면보다 ~1.8m 위
```

입력 데이터 포맷: `float32 × 5 = [x, y, z, intensity, ring]`

---

## 1. Stage 0: 전처리 (Preprocessing)

> 파일: `geometry.py:preprocess()`

원본 점 중에는 파이프라인에 해로운 점들이 있다. 이 단계에서 두 종류를 제거한다.

### 1-1. 자차(Ego-vehicle) 마스크

LiDAR는 360도로 스캔하므로, 자기 차량 표면에서 반사된 점들이 포함된다. 이 점들은 의미 없으므로 제거한다.

```
d(p) = ||p||₂                  (센서 원점으로부터의 거리)
keep(p) = d(p) > r_ego         (r_ego = 2.5m)
```

자차의 반경이 약 2m이므로, 2.5m 이내의 점은 모두 자차 표면으로 간주한다.

### 1-2. 통계적 이상치 제거 (Statistical Outlier Removal, SOR)

LiDAR 반사 특성상, 먼지나 빗방울에서 반사되거나 다중 반사(multi-path)로 인한 유령 점(ghost point)이 존재한다. 이를 통계적으로 제거한다.

**방법:**

1. 각 점 p에 대해, k=10개 최근접 이웃까지의 **평균 거리**를 구한다:

```
mean_dist(p) = (1/k) × Σᵢ ||p - neighbor_i||
```

2. 모든 점의 mean_dist의 전역 평균과 표준편차를 구한다:

```
μ_global = E[mean_dist(p)]
σ_global = Std[mean_dist(p)]
```

3. 임계값을 초과하는 점은 이상치로 제거한다:

```
inlier(p) ⟺ mean_dist(p) < μ_global + 2.0 × σ_global
```

**직관:** 정상적인 점은 주변에 비슷한 밀도로 점이 있다. 이상치는 주변에 점이 적으므로 mean_dist가 비정상적으로 크다. 2σ 규칙은 정규분포에서 약 95%를 포함하므로, 상위 ~5%의 극단적으로 고립된 점을 제거한다.

**결과:** 34,720 → 22,022 점 (약 37% 제거, 대부분 자차 + 원거리 노이즈)

---

## 2. Stage 1: 국소 기하 추정 (Local Geometry Estimation)

> 파일: `geometry.py:estimate_local_geometry()`

각 점에서 표면의 국소적 성질(법선 방향, 곡률, 평탄도)을 추정한다. 이 정보는 이후 모든 단계에서 사용된다.

### 2-1. K최근접 이웃 탐색 (KNN)

각 점의 주변 k=30개 이웃을 찾는다. 이 이웃 집합이 해당 점 주변의 국소 표면을 대표한다.

```
N(p) = {k개 가장 가까운 점들}    (k = 30)
```

구현은 `_knn_points()`에서 pytorch3d → torch_cluster → brute-force 순으로 가용한 라이브러리를 사용한다. torch_cluster가 설치되어 있으면 CUDA 커널을 사용하여 O(N log N) 수준으로 가속된다.

### 2-2. 공분산 행렬과 PCA

이웃 점들의 분포를 3×3 **공분산 행렬(covariance matrix)**로 요약한다.

```
centered = N(p) - mean(N(p))           (이웃 점 중심화)
Σ_p = (1/(k-1)) × centeredᵀ × centered   (3×3 공분산 행렬)
```

이 행렬에 **특이값 분해(SVD)**를 적용한다:

```
Σ_p = U × diag(λ₁, λ₂, λ₃) × Vᵀ      (λ₁ ≥ λ₂ ≥ λ₃ ≥ 0)
```

여기서 λ₁, λ₂, λ₃는 이웃 점 분포의 **세 주축 방향 분산**이다.

**기하학적 의미:**

```
     λ₁ 방향: 점들이 가장 넓게 퍼진 방향 → 탄젠트 벡터 u
     λ₂ 방향: 두 번째로 퍼진 방향         → 탄젠트 벡터 v
     λ₃ 방향: 점들이 가장 좁게 분포        → 법선 벡터 n

     평면 위의 점들:  λ₁ ≈ λ₂ >> λ₃ ≈ 0   (디스크 모양)
     직선 위의 점들:  λ₁ >> λ₂ ≈ λ₃ ≈ 0   (막대 모양)
     구형 분포:       λ₁ ≈ λ₂ ≈ λ₃        (공 모양)
```

### 2-3. 법선, 곡률, 평탄도

SVD 결과에서 세 가지 특성을 추출한다:

**법선(normal):** λ₃에 대응하는 고유벡터 (최소 분산 방향)
```
n_p = v₃
```
직관: 평면 위 점들은 평면에 수직인 방향으로 거의 퍼지지 않는다. 따라서 최소 분산 방향이 법선이다.

**곡률(curvature):** 최소 고유값의 상대적 크기
```
κ(p) = λ₃ / (λ₁ + λ₂ + λ₃)
```
직관: 평면이면 λ₃ ≈ 0이므로 κ ≈ 0. 곡면이나 모서리에서는 λ₃가 커지므로 κ가 커진다. 값 범위는 [0, 1/3].

**평탄도(planarity):** 표면이 얼마나 판 모양인지
```
π(p) = (λ₂ - λ₃) / λ₁
```
직관: λ₂와 λ₃가 비슷하면(직선이나 구) 평탄도가 낮고, λ₂ >> λ₃이면(판) 높다.

### 2-4. 법선 방향 통일

SVD에서 나온 법선은 부호가 임의적이다(n과 -n 모두 가능). 센서 방향으로 통일한다:

```
if n_p · (-p) < 0:        (p 자체가 센서에서 점까지의 벡터)
    n_p ← -n_p            (센서 쪽을 향하도록 뒤집음)
    v_p ← -v_p            (오른손 좌표계 유지를 위해 탄젠트도 뒤집음)
```

---

## 3. Stage 2: 시드 배치 (Seed Placement)

> 파일: `seeding.py`, `ground.py`

클러스터의 **초기 중심점(seed)**을 배치한다. 시드의 개수와 위치가 최종 클러스터 수와 품질을 크게 좌우한다.

### 3-0. 지면 검출 (Ground Detection)

> 파일: `ground.py:detect_ground()`

지면과 비지면은 기하학적 특성이 다르므로, 분리하여 처리한다.

**3단계 필터:**

| 단계 | 기준 | 수식 | 의미 |
|------|------|------|------|
| 1 | Z 범위 | `-3.0 ≤ z_p ≤ -1.0` | 물리적으로 지면일 수 있는 높이 |
| 2 | 법선 방향 | `\|n_p · [0,0,1]\| > 0.9` | 법선이 수직에서 ~26° 이내 |
| 3 | RANSAC | 피팅 평면에서 0.15m 이내 | 실제로 하나의 평면 위에 있는지 |

세 기준을 **모두** 통과한 점만 ground로 분류한다.

**RANSAC (RANdom SAmple Consensus):**

아웃라이어가 많은 데이터에서 모델을 피팅하는 반복적 방법이다.

```
50회 반복:
  1. 후보 점 중 무작위 3점 선택 (평면 정의에 필요한 최소 수)
  2. 이 3점으로 평면 방정식 결정:
       n = (p₁ - p₀) × (p₂ - p₀)       (외적 → 평면 법선)
       n = n / ||n||                     (정규화)
  3. 모든 후보 점의 평면까지 거리:
       d(q) = |n · (q - p₀)|
  4. d(q) < 0.15m인 점 = inlier
  5. inlier 수가 최대이면 이 평면을 best로 저장
```

최소제곱법(least squares)과의 차이: 최소제곱은 모든 점을 사용하므로 아웃라이어 하나가 결과를 크게 왜곡한다. RANSAC은 무작위 소표본만 사용하므로 아웃라이어에 강건하다.

### 3-1. 적응형 복셀 크기 계산

> 파일: `seeding.py:compute_adaptive_voxel_size()`

3D 공간을 균일한 격자(voxel grid)로 나누고, 각 격자에서 하나의 시드를 뽑는다. 격자 크기가 클러스터 크기를 결정한다.

```
V_bbox = (x_max - x_min)(y_max - y_min)(z_max - z_min)    (바운딩 박스 부피)
n_target = N / target_cluster_size(hyperparameters)         (목표 복셀 수)
V_voxel = V_bbox / n_target                                 (복셀 하나의 부피)
h = max(V_voxel^(1/3), 0.1)                                (복셀 한 변의 길이)
```

target_cluster_size = 30이면, 평균적으로 복셀 하나에 30개 점이 들어가도록 격자 크기를 결정한다.

**지면/비지면 차별화:**

```
지면:     h_ground = h × 3.0     (큰 복셀 → 적은 시드 → 큰 클러스터)
비지면:   h_nonground = h        (기본 복셀 → 많은 시드 → 작은 클러스터)
```

지면은 넓고 평탄하므로 큰 서펠로 충분하다. 차량, 보행자 등은 복잡하므로 작은 서펠이 필요하다.

### 3-2. 복셀 시딩 (Voxel Seeding)

> 파일: `seeding.py:voxel_seeding()`

각 복셀에서 **복셀 중심에 가장 가까운 점**을 시드로 선택한다.

```
1. 각 점의 복셀 좌표: voxel(p) = ⌊p / h⌋
2. 복셀 ID 인코딩:    id = i_x × (dim_y × dim_z) + i_y × dim_z + i_z
3. 복셀별 중심(centroid): c_v = mean(점들 in 복셀 v)
4. 시드 선택: s_v = argmin_{p ∈ v} ||p - c_v||²
```

구현 기법: 정렬(sorting) 기반으로 Python 루프 없이 벡터화. (voxel_id × 1e10 + distance)를 정렬 키로 사용하여, 각 복셀의 최소 거리 점을 O(N log N)에 찾는다.

### 3-3. 고곡률 FPS 보강

> 파일: `seeding.py:farthest_point_sampling()`

복셀 시딩만으로는 곡률이 높은 영역(모서리, 물체 경계)에 시드가 부족할 수 있다. 이를 보완한다.

```
1. 곡률 상위 15% 점 추출: high_curv = {p : κ(p) > κ₀.₈₅}
2. FPS로 n_extra개 추가 시드 선택 (n_extra = 0.1 × |voxel_seeds|)
3. 최종 시드 = voxel_seeds ∪ fps_seeds (중복 제거)
```

**FPS (Farthest Point Sampling):**

```
p₀ = 임의의 점
p_i = argmax_p min_{j<i} ||p - p_j||²
```

매 반복마다 "기존 선택된 점들과 가장 먼 점"을 선택한다. 결과적으로 공간을 **균일하게 커버**하는 점 집합을 얻는다. torch_cluster가 설치되어 있으면 CUDA 커널로 가속된다.

---

## 4. Stage 3: 포인트 할당 (Point-to-Seed Assignment)

> 파일: `assignment.py:assign_points()`

22,022개의 모든 점을 ~500개 시드 중 하나에 할당한다. 할당 기준은 **비용 함수(cost function)**이다.

### 4-1. 비용 함수 구성

각 점 p에 대해, 가장 가까운 C=20개 시드를 후보로 선택한다. 각 후보 시드 s에 대해 세 가지 비용을 계산한다:

**공간 비용 (Spatial Cost):**

```
C_spatial(p, s) = ||p - s||² / σ²
```

σ²는 **자동 조정(auto-tune)**된다:
```
σ² = median_p(||p - nearest_seed(p)||²)
```
모든 점에서 가장 가까운 시드까지의 거리 제곱의 중앙값. 이렇게 하면 시드 밀도에 따라 자동으로 스케일이 조정된다.

**법선 비용 (Normal Cost):**

```
C_normal(p, s) = 1 - (n_p · n_s)²
```

| n_p · n_s | 기하학적 의미 | C_normal |
|-----------|--------------|----------|
| ±1 | 평행 (같은 표면) | 0 |
| 0 | 수직 (다른 표면) | 1 |

제곱을 사용하는 이유: 법선의 부호는 임의적일 수 있다(n과 -n 모두 유효). 제곱하면 부호에 무관하게 방향만 비교한다.

**곡률 비용 (Curvature Cost):**

```
C_curvature(p, s) = (κ_p - κ_s)² / E[(κ_p - κ_s)²]
```

분모로 나누어 **스케일 불변(scale-invariant)**하게 만든다. 곡률 값의 절대 크기가 아닌 상대적 차이만 본다.

### 4-2. 가중 합산 및 할당

```
C_total(p, s) = C_spatial + 0.3 × C_normal + 0.1 × C_curvature
cluster(p) = argmin_{s ∈ candidates(p)} C_total(p, s)
```

| 가중치 | 의미 |
|--------|------|
| 1.0 (spatial) | 가까운 시드를 기본으로 선호 |
| 0.3 (normal) | 같은 표면 방향이면 약간 더 선호 |
| 0.1 (curvature) | 같은 곡률이면 조금 더 선호 |

공간 비용이 지배적이므로 기본적으로 **가까운 점끼리 같은 클러스터**에 들어가되, 표면 방향이 다르면(예: 벽과 바닥의 경계) 다른 클러스터로 분리된다.

---

## 5. Stage 4: 리파인먼트 (Split/Merge Refinement)

> 파일: `refinement.py:refine_clusters()`

초기 할당은 시드 위치에 의존하므로 최적이 아닐 수 있다. **분할(split)**과 **병합(merge)**을 반복하여 클러스터를 정제한다.

```
iteration 0: SPLIT (짝수)
iteration 1: MERGE (홀수)
iteration 2: SPLIT
iteration 3: MERGE
(최대 4회, 조기 종료 가능)
```

### 5-1. 클러스터 통계량

> 파일: `refinement.py:_cluster_stats()`

Split/Merge 판단을 위해 각 클러스터의 통계를 계산한다.

**중심(centroid):**
```
μ_k = (1/|C_k|) × Σ_{p ∈ C_k} p
```

**클러스터 법선:**
```
n_k = normalize( Σ_{p ∈ C_k} n_p )     (포인트 법선의 평균, 재정규화)
```

**gamma_rms (핵심 품질 지표):**
```
γ_p = n_k · (p - μ_k)                  (점 p의 탄젠트 평면으로부터의 부호 거리)
γ_rms(k) = √( (1/|C_k|) × Σ_{p ∈ C_k} γ_p² )
```

gamma_rms는 클러스터 내 점들이 피팅된 평면에서 **얼마나 벗어나는지**의 RMS(Root Mean Square)이다. 이 값이 작을수록 서펠이 표면을 잘 근사한다.

**조건수(condition number):**
```
Σ_k = (1/(|C_k|-1)) × Σ_{p ∈ C_k} (p - μ_k)(p - μ_k)ᵀ     (3×3 공분산)
SVD: λ₁ ≥ λ₂ ≥ λ₃
κ(k) = λ₁ / λ₃
```

조건수가 크면 클러스터가 매우 길쭉하거나 거의 1차원적(직선)이어서, 안정적인 평면 피팅이 불가능하다.

### 5-2. Split (분할)

**분할 기준 — 하나라도 해당되면 분할:**

| 기준 | 조건 | 의미 |
|------|------|------|
| 평면 적합 | γ_rms > 0.05m | 점들이 평면에서 5cm 이상 벗어남 |
| 크기 | \|C_k\| > 80 | 너무 많은 점 → 여러 표면을 걸칠 수 있음 |
| 형상 | κ > 20 | 너무 길쭉함 → 안정적 피팅 불가 |
| 최소 크기 | \|C_k\| ≥ 10 | 분할 후 각 반이 최소 5개 이상이어야 함 |

**분할 방법 — 2-means:**

```
1. 초기화: 클러스터 내 가장 먼 두 점을 중심으로 설정
2. 10회 반복:
     - 각 점을 가까운 중심에 할당
     - 중심을 할당된 점들의 평균으로 갱신
3. 결과: 절반의 점이 새 클러스터 ID를 받음
```

### 5-3. Merge (병합)

서로 다른 복셀에서 시드가 생성되었지만, 실제로는 같은 표면인 인접 클러스터를 합친다.

**병합 후보 탐색:** 각 클러스터의 centroid에서 KNN(k=6)으로 이웃 클러스터를 찾는다.

**병합 기준 — 모두 만족해야 병합:**

| 기준 | 조건 | 의미 |
|------|------|------|
| 법선 각도 | angle(n_i, n_j) < 5° | 같은 방향의 표면 |
| 거리 | \|\|μ_i - μ_j\|\| < 2.0 × d_median | 공간적으로 가까움 |
| 합산 크기 | \|C_i\| + \|C_j\| ≤ 80 | 합쳐도 너무 크지 않음 |
| 품질 유지 | γ_rms(C_i ∪ C_j) < 0.05 | 합쳐도 평면 적합이 유지됨 |

여기서 d_median은 모든 클러스터 centroid 간 **최근접 이웃 거리의 중앙값**이다. 이를 기준으로 "가까움"을 정의하면, 클러스터 밀도에 적응적이다.

**왜 거리 기준이 필요한가:** 법선 각도만으로 판단하면, 평행하지만 멀리 떨어진 두 벽(예: 건물 양쪽)이 잘못 병합된다.

### 5-4. 수렴 판단

```
Δ = (변경된 점 수) / (전체 점 수)
if Δ < 0.01:    (1% 미만 변경)
    break       (조기 종료)
```

### 5-5. Post-Refinement 재할당

Split/Merge 후 클러스터 경계가 바뀌었으므로, 경계 근처의 점들이 최적이 아닐 수 있다. 모든 점에 대해 Stage 3과 동일한 비용 함수로 재할당한다.

```
새 centroids = 현재 클러스터 중심
새 normals = 현재 클러스터 법선

각 점 p에 대해:
    k개 가장 가까운 centroid 중
    C_total = C_spatial + 0.3 × C_normal이 최소인 클러스터로 재배치
```

---

## 6. Stage 5: 2D 가우시안 피팅 (2D Gaussian Fitting)

> 파일: `gaussian_fit.py:fit_2d_gaussians()`

최종 클러스터 각각을 하나의 2D 가우시안 서펠로 변환한다.

### 6-1. 클러스터 중심 (Position)

```
μ_k = (1/|C_k|) × Σ_{p ∈ C_k} p      (가우시안의 3D 위치)
```

### 6-2. 탄젠트 프레임 (Tangent Frame)

클러스터 내 점들의 3D 공분산에 SVD를 적용:

```
Σ_k = (1/(|C_k|-1)) × Σ (p - μ_k)(p - μ_k)ᵀ
SVD → u_k (주축), v_k (부축), n_k (법선)
```

이 세 벡터가 서펠의 **국소 좌표계**를 형성한다.

### 6-3. 재파라미터화 (Reparameterization)

각 점을 국소 좌표계로 변환:

```
α_p = u_k · (p - μ_k)     탄젠트 방향 1 (서펠 위)
β_p = v_k · (p - μ_k)     탄젠트 방향 2 (서펠 위)
γ_p = n_k · (p - μ_k)     법선 방향 (서펠에서 벗어난 거리)
```

(u, v, n)이 정규직교기저이므로, 재구성이 무손실이다:

```
p = μ_k + α_p × u_k + β_p × v_k + γ_p × n_k      (정확히 원래 점으로 복원)
```

### 6-4. 2D 스케일링 (Scaling)

탄젠트 평면 위의 2D 공분산에서 서펠의 크기를 결정:

```
Σ_k^(2D) = (1/(|C_k|-1)) × Σ [α_p, β_p]ᵀ [α_p, β_p]      (2×2 행렬)

고유값: λ₁ ≥ λ₂ > 0
스케일링: s_k = [√λ₁, √λ₂]       (장축, 단축 반지름)
```

직관: λ₁이 크면 점들이 한 방향으로 길게 퍼져 있다 → 타원형 서펠. λ₁ ≈ λ₂이면 원형 서펠.

### 6-5. 회전 (Rotation → Quaternion)

탄젠트 프레임을 쿼터니언으로 변환:

```
R_k = [u_k | v_k | n_k]         (3×3 회전 행렬)
q_k = mat_to_quat(R_k)          (w, x, y, z 순서의 단위 쿼터니언)
```

kornia 라이브러리를 사용하여 수치적으로 안정적인 변환을 수행한다. kornia는 (x,y,z,w) 순서를 반환하므로, (w,x,y,z) 순서로 재배열한다.

### 6-6. 불투명도 (Opacity)

서펠의 품질에 따라 불투명도를 결정:

```
α_k = exp(-10 × γ_rms(k))       (0.01 ≤ α_k ≤ 0.99로 클램핑)
```

| γ_rms | α (opacity) | 의미 |
|-------|-------------|------|
| 0.0 | 1.0 (불투명) | 완벽한 평면 → 확신 높음 |
| 0.05 | 0.61 | 양호한 적합 |
| 0.1 | 0.37 | 보통 |
| 0.3 | 0.05 (거의 투명) | 나쁜 적합 → 확신 낮음 |

직관: 평면 근사가 나쁜 서펠은 반투명하게 만들어, 렌더링 시 영향을 줄인다.

### 6-7. 유효성 필터

```
유효 클러스터: |C_k| ≥ 5     (min_cluster_size)
```

5개 미만의 점으로는 3×3 공분산 행렬을 신뢰성 있게 추정할 수 없다. (3×3 대칭 행렬의 자유 파라미터 6개 + centroid 3개 = 9개. 점 5개 × 좌표 3개 = 관측 15개. 최소한의 자유도 확보.)

---

## 7. 품질 지표 (Quality Metrics)

파이프라인의 출력 품질을 평가하는 지표들이다.

### 7-1. gamma_rms — 평면 적합도

```
γ_rms(k) = √( (1/|C_k|) × Σ γ_p² )
```

각 서펠이 실제 표면을 얼마나 잘 근사하는지의 **핵심 지표**이다. Stage 4의 split 기준(0.05m)이기도 하고, Stage 5의 opacity 결정에도 사용된다. 단위는 미터.

### 7-2. Coverage — 할당 완전성

```
coverage = |{p : valid cluster에 할당됨}| / N_filtered
```

가우시안으로 표현되지 못한 점이 얼마나 되는지. 99% 이상이 목표.

### 7-3. Condition Number — 형상 안정성

```
κ(k) = λ₁ / λ₃
```

높으면 클러스터가 퇴화(degenerate)되어 피팅이 불안정하다. Stage 4에서 κ > 20이면 split한다.

### 7-4. Reparameterization Error — 구현 정합성

```
error = ||p - (μ + α·u + β·v + γ·n)||
```

이론적으로 정확히 0이어야 한다 (정규직교기저 분해이므로). 이 값이 0이 아니면 코드에 버그가 있다는 뜻이다. 품질 지표가 아니라 **디버깅용 sanity check**이다.

---

## 8. 최종 출력 구조

```python
{
    # ===== 가우시안 파라미터 (K'개, valid clusters) =====
    "xyz":         [K', 3],     # 중심 위치
    "rotation":    [K', 4],     # 쿼터니언 (w, x, y, z)
    "scaling":     [K', 2],     # 2D 스케일 (장축, 단축)
    "normal":      [K', 3],     # 표면 법선
    "opacity":     [K', 1],     # 불투명도
    "intensity":   [K', 1],     # 평균 반사 강도
    "tangent_u":   [K', 3],     # 탄젠트 벡터 1
    "tangent_v":   [K', 3],     # 탄젠트 벡터 2

    # ===== 시간 관련 (NVS용 placeholder) =====
    "velocity":    [K', 3],     # 속도 (초기값 0)
    "t_center":    [K', 1],     # 시간 중심
    "scaling_t":   [K', 1],     # 시간 스케일

    # ===== 재파라미터화 (역추적용) =====
    "point_assignments": [N],   # 각 점의 클러스터 ID
    "alphas":            [N],   # 탄젠트 좌표 α
    "betas":             [N],   # 탄젠트 좌표 β
    "gammas":            [N],   # 법선 좌표 γ
    "point_mask":        [N],   # 유효 클러스터 소속 여부

    # ===== 내부 통계 =====
    "_counts":     [K'],        # 클러스터별 점 수
    "_gamma_rms":  [K'],        # 클러스터별 평면 적합도
    "_cov_2d":     [K', 2, 2],  # 2D 공분산 행렬
}
```

---

## 9. 하이퍼파라미터 전체 목록

> 파일: `config.py`

| 파라미터 | 값 | Stage | 의미 |
|----------|-----|-------|------|
| `ego_radius` | 2.5m | 0 | 자차 마스크 반경 |
| `sor_k` | 10 | 0 | SOR 이웃 수 |
| `sor_std_mul` | 2.0 | 0 | SOR 임계값 = μ + 2σ |
| `knn_k` | 30 | 1 | PCA용 이웃 수 |
| `target_cluster_size` | 30 | 2 | 복셀당 목표 점 수 |
| `curvature_quantile` | 0.85 | 2 | 고곡률 기준 (상위 15%) |
| `fps_extra_ratio` | 0.1 | 2 | FPS 추가 시드 비율 |
| `candidate_k` | 20 | 3 | 후보 시드 수 |
| `lambda_normal` | 0.3 | 3 | 법선 비용 가중치 |
| `lambda_curvature` | 0.1 | 3 | 곡률 비용 가중치 |
| `max_refine_iters` | 4 | 4 | Split/Merge 반복 횟수 |
| `split_gamma_rms` | 0.05m | 4 | Split 평면 적합 기준 |
| `split_max_size` | 80 | 4 | Split 크기 기준 |
| `split_cond_number` | 20.0 | 4 | Split 조건수 기준 |
| `merge_angle_deg` | 5.0° | 4 | Merge 법선 각도 기준 |
| `merge_max_dist_ratio` | 2.0 | 4 | Merge 거리 기준 (× 중앙값) |
| `convergence_threshold` | 0.01 | 4 | 수렴 판단 (1% 미만 변경) |
| `min_cluster_size` | 5 | 5 | 최소 유효 클러스터 크기 |
| `ground_z_range` | (-3, -1)m | 2 | 지면 후보 z 범위 |
| `ground_normal_thresh` | 0.9 | 2 | 지면 법선 기준 (cos) |
| `ground_voxel_scale` | 3.0 | 2 | 지면 복셀 배율 |

---

## 10. 실측 결과 (nuScenes 단일 프레임)

```
입력:            34,720 점
전처리 후:       22,022 점
출력 가우시안:   395 개
Coverage:        99.4%
Ground ratio:    41.8%
gamma_rms:       median 0.053, max 0.520
Reparam error:   max 8.5e-6 (사실상 0)
GPU peak:        54.6 MB
총 소요시간:     11.7초 (refinement 10초 병목)
```

---

## 부록 A: 파일 구조

```
gaustering/
├── __init__.py          # 패키지 선언
├── config.py            # 하이퍼파라미터 정의
├── data_loader.py       # nuScenes .pcd.bin 로딩
├── geometry.py          # Stage 0-1: 전처리, KNN, PCA
├── ground.py            # 지면 검출 (RANSAC)
├── seeding.py           # Stage 2: 복셀 시딩, FPS
├── assignment.py        # Stage 3: 비용 기반 할당
├── refinement.py        # Stage 4: Split/Merge 정제
├── gaussian_fit.py      # Stage 5: 2D 가우시안 피팅
├── pipeline.py          # 전체 파이프라인 통합
├── visualization.py     # 통계 플롯, BEV, 히트맵
├── test_nuscenes.py     # 단일 프레임 테스트
├── eval_batch.py        # 배치 평가
├── viz_server.py        # 3D 인터랙티브 뷰어
└── environment.yml      # Conda 환경 정의
```

## 부록 B: 비용 함수 요약

```
                    Stage 3 (할당)                  Stage 4 (리파인먼트)

공간         ||p - s||² / σ²                 ─
법선         1 - (n_p · n_s)²                angle(n_i, n_j) < 5°
곡률         (κ_p - κ_s)² / E[·]            ─
평면 적합    ─                               γ_rms > 0.05
형상         ─                               λ₁/λ₃ > 20
크기         ─                               |C_k| > 80
거리         ─                               ||μ_i - μ_j|| < 2 × d_median

가중 합:     1.0 × spatial                   Split: OR 조건 (하나라도 해당)
             + 0.3 × normal                  Merge: AND 조건 (모두 만족)
             + 0.1 × curvature
```
