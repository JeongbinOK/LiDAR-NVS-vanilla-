# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# CUDA extension build output. These are per-GPU-architecture binaries: a
# committed sm_89 .so is imported in preference to a rebuild and then fails on
# any other card, so let each machine JIT its own.
src/models_new/utils/chamfer/tmp/

# Checkpoints / Models
*.pt
*.pth

logs/
lightning_logs/
.tmp/
.cache/
*.txt
tools/

# Outputs / Logs / Visualization
outputs/
ckpt/
*.png
*.html
*.log
*.pdf

# Offline detection + tracking results (LK3D + MCTrack, large files)
bbox/

# External detector/tracker repos (cloned on demand by run_all.sh)
nn/detector/OpenPCDet/
nn/detector/MCTrack/

# Rasterizer build artifacts
renderer/lidar_qgs_rasterizer/build/
renderer/lidar_qgs_rasterizer/diff_quadratic_rasterization/_C*.so

# Environments
.env
.venv
env/
venv/
ENV/
.codex

# Local-only agent/editor/workflow files
.agents/
.claude/
.vscode/
AGENTS.md
CLAUDE.md

# Local-only experiments and validation helpers
scripts/
tests/*
!tests/test_grid_query_head.py
!tests/test_anchor_modes.py
!tests/test_config_loader.py
!tests/test_dynamic_gaussian.py
!tests/run_dynamic_smoke.py
test_bbox_viz.py
inspect_overfit_pair.py
viz.py
visualize_qgs_surfaces.py

# Local-only method notes
docs_method/
docs/
